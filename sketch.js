var box;

function setup() {
  createCanvas(400,400);
  box = createSprite(200,300,);

}

function draw() 
{
   background(150);

  box.position.x = box.position.x = -3;
  box.position.y = box.position.y = +1;

drawSprites();
}
